---
title: <%= functionName %>
---

# <%= functionName %>

<% if (documentation['Documentation.Description']) { %>
## Description

<%= documentation['Documentation.Description'] %>
<% } %>

## Syntax

```powerquery
<%= functionName %>(<% 
let totalParams = Object.keys(parameters).length;
let reqParams = parseInt(requiredParameters, 10);
Object.entries(parameters).forEach(([key, type], index) => {
    let optionalStr = index >= reqParams ? 'optional ' : '';
%><%= optionalStr %><%= key %> as <%= type %><%= index < totalParams - 1 ? ', ' : '' %><% 
}); %>) as <%= returnType %>
```

<% if (documentation['Documentation.LongDescription']) { %>
## Details

<%- documentation['Documentation.LongDescription'] %>
<% } %>

<% if (documentation.Examples && documentation.Examples.length > 0) { %>
## Examples

<% documentation.Examples.forEach(function(example) { %>
### Example: <%= example.Description %>

```powerquery
<%= example.Code %>
```

Result: <%= example.Result %>

<% }); %>
<% } %>