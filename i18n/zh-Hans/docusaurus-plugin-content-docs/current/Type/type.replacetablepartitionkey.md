---
title: Type.ReplaceTablePartitionKey
---

# Type.ReplaceTablePartitionKey


返回一个新的表类型，其中分区键替换为指定的分区键。


## Syntax

```powerquery
Type.ReplaceTablePartitionKey(
    tableType as type,
    partitionKey as list
) as type
```


## Remarks

返回一个新的表类型，其中分区键替换为指定的分区键。



## Category
Type
