---
title: Cube.ApplyParameter
---

# Cube.ApplyParameter


在将参数应用到多维数据集后将其返回。


## Syntax

```powerquery
Cube.ApplyParameter(
    cube as table,
    parameter as any,
    optional arguments as list
) as table
```


## Remarks

通过 `arguments` 将 `parameter` 应用到 `cube` 后返回多维数据集。



## Category
Cube
