---
title: Number.Atan2
---

# Number.Atan2


返回两个数相除的反正切。


## Syntax

```powerquery
Number.Atan2(
    y as number,
    x as number
) as number
```


## Remarks

返回角度(以弧度为单位)，其正切值为 `y` 和 `x` 两个数字的商 `y` / `x`。



## Category
Number.Trigonometry
