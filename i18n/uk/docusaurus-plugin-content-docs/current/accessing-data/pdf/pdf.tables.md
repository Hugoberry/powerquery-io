---
title: Pdf.Tables
---

# Pdf.Tables


Повертає всі таблиці, знайдені в PDF-файлі.


## Syntax

```powerquery
Pdf.Tables(
    pdf as binary,
    optional options as record
) as table
```


## Remarks

Повертає всі таблиці, знайдені в: `pdf`. Можна вказати додатковий параметр запису `options`, щоб визначити додаткові властивості. Запис може містити описані нижче поля.

-   `Implementation` : Версія алгоритму, яку потрібно використовувати для ідентифікації таблиць. Старі версії доступні лише задля зворотної сумісності, зокрема щоб оновлення алгоритму не порушувало роботу попередніх запитів. Використання найновішої версії завжди забезпечує найкращі результати. Припустимі значення: "1.3", "1.2", “1.1” і Null-значення.
-   `StartPage` : Указує першу сторінку в діапазоні для перевірки. Стандартне значення: 1.
-   `EndPage` : Указує останню сторінку в діапазоні для перевірки. Стандартне значення: остання сторінка документа.
-   `MultiPageTables` : Визначає, чи схожі таблиці на послідовних сторінках автоматично комбінуються в єдину таблицю. Стандартне значення: true.
-   `EnforceBorderLines` : Визначає, чи лінії меж завжди примусово застосовуються як границі клітинок (значення true), чи просто пропонуються як один із варіантів границь (значення false). Стандартне значення: false.


## Examples

### Example #1
Повертає таблиці, які містяться у файлі sample.pdf.
```powerquery
Pdf.Tables(File.Contents("c:\sample.pdf"))
```

Result: 
```powerquery
#table({"Name", "Kind", "Data"}, ...)
```




## Category
Отримання доступу до даних
