---
title: Number.Atan2
---

# Number.Atan2


2 つの数値の間で除算を行った結果のアークタンジェントを返します。


## Syntax

```powerquery
Number.Atan2(
    y as number,
    x as number
) as number
```


## Remarks

2 つの数値 `y`と`x` の商 `y`/`x` を正接とする角度をラジアン単位で返します。



## Category
Number.Trigonometry
