---
title: Vertica.Database
---

# Vertica.Database


Импорт данных из Vertica


## Syntax

```powerquery
Vertica.Database(
    server as text,
    database as text,
    optional options as record
) as table
```


## Remarks

Возвращает таблицу схем, доступных на сервере, имя которого указано в параметре `server`, из базы данных с именем в параметре `database`. Чтобы указать дополнительные свойства, можно использовать необязательный параметр записи `options`. Запись может содержать следующие поля:

-   `ConnectionTimeout`: время ожидания, прежде чем попытки подключиться к серверу будут прекращены. Значение по умолчанию зависит от драйвера.
-   `CommandTimeout` : время, как долго может выполняться запрос на стороне сервера, пока он не будет отменен. Значение по умолчанию зависит от драйвера.


## Examples

### Example #1
Перечисление таблиц в Vertica
```powerquery

```



