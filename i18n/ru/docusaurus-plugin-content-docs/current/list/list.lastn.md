---
title: List.LastN
---

# List.LastN


Возвращает список последних элементов в указанном списке. Дополнительно можно указать количество возвращаемых значений или условие соответствия.


## Syntax

```powerquery
List.LastN(
    list as list,
    optional countOrCondition as any
) as any
```


## Remarks

Возвращает список последнего элемента или последних элементов в указанном списке.

-   `list`: список для проверки. Если список пуст, возвращается пустой список.
-   `countOrCondition`: (необязательно) поддерживает сбор нескольких элементов или фильтрацию элементов. Хотя этот параметр указан как необязательный, возникнет ошибка, если это значение не указано или равно `null`. Этот параметр можно указать двумя способами:
    -   Если указано число, возвращается количество элементов, меньшее этого числа или равное ему.
    -   Если указано условие, возвращаются все элементы, которые соответствуют условию, начиная с конца списка. Как только попадается элемент, не соответствующий условию, дальнейшие элементы не рассматриваются.


## Examples

### Example #1
Найти последнее значение в списке \{3, 4, 5, -1, 7, 8, 2\}.
```powerquery
List.LastN({3, 4, 5, -1, 7, 8, 2}, 1)
```

Result: 
```powerquery
{2}
```


### Example #2
Найти последние значения в списке \{3, 4, 5, -1, 7, 8, 2\}, превышающие 0.
```powerquery
List.LastN({3, 4, 5, -1, 7, 8, 2}, each _ > 0)
```

Result: 
```powerquery
{7, 8, 2}
```




## Category
List.Selection
